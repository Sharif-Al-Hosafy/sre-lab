name: Just Print

on:
    push:
        branches:
            - master

jobs:
    print-job:
        runs-on: ubuntu-latest

        steps:
            - name: print hello
              run: echo "Hello World!"

            - name: checkout repo
              uses: actions/checkout@v4.2.2

            - name: list files
              run: ls -la
